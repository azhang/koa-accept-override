{
  "name": "koa-accept-override",
  "version": "0.0.1",
  "description": "Middleware to override accept header with query or url path file extension",
  "main": "index.js",
  "scripts": {
    "test": "eslint . && istanbul cover _mocha --dir $CIRCLE_ARTIFACTS -- test/**/*.js -R spec",
    "coveralls": "cat $CIRCLE_ARTIFACTS/lcov.info | coveralls",
    "publish-new": "publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/azhang/koa-accept-override.git"
  },
  "keywords": [
    "koa",
    "middleware",
    "accept",
    "content-type"
  ],
  "author": "Allie Zhang",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/azhang/koa-accept-override/issues"
  },
  "homepage": "https://github.com/azhang/koa-accept-override#readme",
  "devDependencies": {
    "coveralls": "^2.11.9",
    "eslint": "^2.11.1",
    "istanbul": "^0.4.0",
    "koa": "^2.0.0",
    "koa-bodyparser": "^3.1.0",
    "mocha": "^2.3.3",
    "publish": "^0.6.0",
    "supertest-koa-agent": "^0.2.1"
  },
  "dependencies": {
    "mime-types": "^2.1.7"
  }
}
